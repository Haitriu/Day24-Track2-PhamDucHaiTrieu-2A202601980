requests>=2.31,<3
pytest>=8.0,<10

# Stretch goal #2 only (scripts/compare_pii_ner.py) — KHÔNG cần cài để
# hoàn thành lab ở mức yêu cầu, agent/pii.py dùng thuần regex. Không có
# model tiếng Việt chính thức cho spaCy (xem reports/pii-ner-comparison.md):
# sau khi cài, chạy thêm `python -m spacy download en_core_web_sm`.
presidio-analyzer
spacy

# Chỉ cần nếu dùng --model claude-... thay cho --mock (không bắt buộc,
# và KHÔNG dùng để chấm điểm — xem day24-LAB-SPEC.md §2).
# anthropic
